package agency.highlysuspect.unfortunately.net;

import net.minecraft.util.Identifier;
import agency.highlysuspect.unfortunately.Unfortunately;

public class UFNet {
	static final Identifier OPEN_RESULT_SCREEN = new Identifier(Unfortunately.MODID, "open_result_screen");
}
